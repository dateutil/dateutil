Fixed an always-failing branch in `tests/property/test_tz_prop.py`. When the converted datetime lands in an ambiguous local hour, the test asserted that `fold` changed the result of `astimezone()` on the *input* -- but the input is UTC-aware and so never ambiguous, making the assertion false whenever it was reached. The check now confirms the ambiguity on the converted value. Reported and fixed by @<submitter> (gh pr #<PR>)
